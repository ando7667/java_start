package Seminar7_1;

public interface Nums {
    int getNext();          // возвращает следующее по порядку число
    void reset();           // сбрасывает число
    void setStart(int x);   // установить начальный параметр
}
